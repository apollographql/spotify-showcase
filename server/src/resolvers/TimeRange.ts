import { TimeRangeResolvers } from './types';

const resolvers: TimeRangeResolvers = {
  LONG_TERM: 'long_term',
  MEDIUM_TERM: 'medium_term',
  SHORT_TERM: 'short_term',
};

export default resolvers;
