import { PlaybackContextTypeResolvers } from './types';

const resolvers: PlaybackContextTypeResolvers = {
  ARTIST: 'artist',
  ALBUM: 'album',
  PLAYLIST: 'playlist',
  SHOW: 'show',
};

export default resolvers;
