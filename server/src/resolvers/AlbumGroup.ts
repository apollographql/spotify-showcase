import { AlbumGroupResolvers } from './types';

const resolvers: AlbumGroupResolvers = {
  ALBUM: 'album',
  APPEARS_ON: 'appears_on',
  COMPILATION: 'compilation',
  SINGLE: 'single',
};

export default resolvers;
