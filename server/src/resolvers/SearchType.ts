import { SearchTypeResolvers } from './types';

const resolvers: SearchTypeResolvers = {
  ALBUM: 'album',
  ARTIST: 'artist',
  EPISODE: 'episode',
  PLAYLIST: 'playlist',
  TRACK: 'track',
  SHOW: 'show',
};

export default resolvers;
