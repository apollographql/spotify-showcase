query Track($q: String!, $type: [SearchType!]!) {
  search(q: $q, type: $type) {
    tracks {
      edges {
        node {
          name
          id
          durationMs
          popularity
          trackNumber
          album {
            name
          }
          artists {
            name
          }
        }
      }
    }
  }
}