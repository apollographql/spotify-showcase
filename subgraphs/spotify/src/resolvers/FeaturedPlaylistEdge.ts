import { FeaturedPlaylistEdgeResolvers } from '../__generated__/resolvers-types';
import { itself } from './helpers';

export const FeaturedPlaylistEdge: FeaturedPlaylistEdgeResolvers = {
  node: itself(),
};
