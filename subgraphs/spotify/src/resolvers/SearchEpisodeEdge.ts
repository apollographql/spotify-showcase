import { SearchEpisodeEdgeResolvers } from '../__generated__/resolvers-types';
import { itself } from './helpers';

export const SearchEpisodeEdge: SearchEpisodeEdgeResolvers = {
  node: itself(),
};
