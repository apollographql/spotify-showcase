import { FollowedArtistEdgeResolvers } from '../__generated__/resolvers-types';
import { itself } from './helpers';

export const FollowedArtistEdge: FollowedArtistEdgeResolvers = {
  node: itself(),
};
