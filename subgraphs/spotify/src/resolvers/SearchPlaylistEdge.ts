import { SearchPlaylistEdgeResolvers } from '../__generated__/resolvers-types';
import { itself } from './helpers';

export const SearchPlaylistEdge: SearchPlaylistEdgeResolvers = {
  node: itself(),
};
