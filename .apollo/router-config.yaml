cors:
  allow_any_origin: true
headers:
  subgraphs:
    spotify:
      request:
        - propagate:
            matching: "x-api-token"
    playback:
      request:
        - propagate:
            matching: "x-api-token"
include_subgraph_errors:
  all: true
sandbox:
  enabled: true
supergraph:
  introspection: true
  listen: 127.0.0.1:4000
homepage:
  enabled: false