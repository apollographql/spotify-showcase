import * as ContextMenu from '@radix-ui/react-context-menu';

const Separator = () => {
  return (
    <ContextMenu.Separator className="bg-offwhite my-1 h-px bg-white/10" />
  );
};

export default Separator;
